name: Reusable workflow
on: workflow_call # It will run when called by another workflow
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: echo "Deploying and Uploading."